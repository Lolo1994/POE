@model Task_2.Models.ActiveDisasters

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ActiveDisasters</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="DisasterType" class="control-label"></label>
                <select asp-for="DisasterType" asp-items="@ViewBag.disastertypes"></select>
                <span asp-validation-for="DisasterType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
